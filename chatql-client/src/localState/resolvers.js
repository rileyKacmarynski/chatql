//these are local resolvers we can use to query the cache


export const resolvers = {
  // Mutation: {
  //   : (_, variables, { cache, getCacheKey }) => {
  //     const id = getCacheKey({ __typename: 'credential', id: variables.id });

      

  //   }
  // }
}