type Query {
  users: [User!]!
}

type Mutation {
    signup(password: String!, name: String!): AuthPayload

}

type AuthPayload {
    token: String!
    user: User 
}

type User {
  id: ID!
  name: String!
}
