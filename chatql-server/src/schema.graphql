type Query {
  users: [User!]!
}

type Mutation {
    signup(password: String!, username: String!): AuthPayload
}

type AuthPayload {
    token: String!
    user: User 
}

type User {
  id: ID!
  username: String!
}
